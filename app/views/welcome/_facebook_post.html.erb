<div class="individual_post">
  <div class="network_logo">
    <img id="facebook_stream_logo" src="<%= asset_url("FB-f-Logo__blue_50.png") %>">
  </div>
  <div class="post_main">
    <div class="post_header">
      <ul>
        <% if to_from_profile_hash.has_key?(post["from"]["id"]) %>
          <li><img src="<%= to_from_profile_hash[post["from"]["id"]] %>"></li>
        <% end %>
        <li><a href="<%= post["from"]["link_to_profile"] %>"><%= post["from"]["name"] %></a></li>
        <li><%= time_ago_in_words(post["created_time"]) %> ago</li>
      </ul>
    </div>
    <div class="post_content">
      <ul>
        <li><%= post["story"] if post["story"] != nil %></p></li>
        <li><%= post["message"] if post["message"] != nil %></li>
        <li><img src="<%= post["image"]["original_sized_image"] if post["image"] != nil %>"></li>
        <li><%= post["caption_text"] %></li>
      </ul>
      <% if post["story_tags"] != nil %>
        <ul>
          <% post["story_tags"].each do |story_tag| %>
            <li>
              <% story_tag.each do |tag| %></li>
                <li><a href="<%= tag[0]["link_to_profile"] %>"><%= tag[0]["name"] %></a></li>
              <% end %>
          <% end %>
          <li>were tagged in this post.</li>
        </ul>
      <% end %>
      <div class="post_stats">
        <ul>
          <li>Comments: <%= post["comments_count"] %></li>
          <li>Likes: <%= post["likes_count"] %></li>
          <% if post["shares_count"] != nil %>
            <li>Shares: <%= post["shares_count"] %></li>
          <% end %>
          <li>
            <a href="<%= post["link_to_post"] if post["link_to_post"] != nil %>">View on Facebook</a>
          </li>
        </ul>
      </div>
      <% if post["comments"] != nil %>
        <div class="post_comments">
          <ul>
            <% post["comments"].each do |comment| %>
              <li class="commenter_profile_picture"><img src="<%= comments_profile_hash[comment["from"]["id"]] %>"></li>
              <li class="commenter_username">
                <a href="<%= comment["from"]["link_to_profile"] %>" target="_blank"><%= comment["from"]["name"] %></a>
              </li>
              <li><%= time_ago_in_words(comment["created_time"]) %> ago</li>
              <li><%= comment["message"] %></li>
              <li>Likes: <%= comment["like_count"] %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>